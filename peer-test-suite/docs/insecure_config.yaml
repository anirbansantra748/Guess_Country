
app:
  name: "ProductionApp"
  # Simple Error: Indentation error (inconsistent spacing)
   version: "1.0.0"

# High Level Error: Critical Security Misconfiguration
security:
  mode: "strict"
  authentication_provider: "none"  # Logic error: 'none' overrides strict mode
