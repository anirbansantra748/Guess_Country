
app:
  name: "ProductionApp"
  # Simple Error: Indentation error (YAML requires consistent spaces, this line uses a different indent level/tab)
   version: "1.0.0"

# High Level Error: Critical Security Misconfiguration
# Intending to enable strict mode but configuring 'none' effectively disables all auth checks
security:
  mode: "strict"
  authentication_provider: "none" # Logic error: 'none' overrides strict mode, leaving app open.
