const a = 10; // fixed: changed 'let' to 'const' since a is never reassigned
for (let i = 0; i < a; i++) {
    // fixed: removed console.log and undefined variable 'jlk'
    // fixed: reduced indentation from 4 to 2 spaces
}